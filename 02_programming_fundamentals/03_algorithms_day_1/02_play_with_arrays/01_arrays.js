/*
 ## Define arrays and access to their values
 */

// Create an array called `digits` with the number from 0 to 9;
const digits = [];
digits.push(0);
digits.push(1);
digits.push(2);
digits.push(3);
digits.push(4);
digits.push(5);
digits.push(6);
digits.push(7);
digits.push(8);
digits.push(9);



console.log(digits);
// create a constant called `first` for the first cell of your array;
const first = digits[0];
console.log(first);

// create a constant called `last` for the last cell of your array;
const last = digits[9];
console.log(last);

// create a constant called `sixth` for the cell at the index 6 of your array;
const sixth = digits[6];
console.log(sixth);
